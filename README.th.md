# BMI Calculator

> [🇬🇧 English](README.md)

แอปพลิเคชันคำนวณดัชนีมวลกาย (Body Mass Index) ที่พัฒนาด้วย Python และ Tkinter พร้อม GUI ที่ใช้งานง่ายและสวยงาม

## 📋 คุณสมบัติ

- คำนวณค่า BMI จากน้ำหนักและส่วนสูง
- แสดงผลการประเมินสุขภาพตามค่า BMI
  - น้ำหนักน้อย (Underweight): BMI 0-18.5
  - น้ำหนักปกติ (Normal): BMI 18.6-25
  - น้ำหนักเกิน (Overweight): BMI 25.1-30
  - น้ำหนักเกินมาก (Super overweight): BMI มากกว่า 30
- ปรับค่าส่วนสูงและน้ำหนักด้วย Slider แบบเรียลไทม์
- แสดงภาพประกอบที่เปลี่ยนขนาดตามส่วนสูงที่เลือก
- UI ที่สวยงามและใช้งานง่าย

## 🖼️ ภาพหน้าจอ

โปรแกรมมีหน้าตาที่สวยงามพร้อมไอคอนและภาพประกอบ

## 🛠️ เทคโนโลยีที่ใช้

- **Python 3.x**
- **Tkinter** - สำหรับสร้าง GUI
- **Pillow (PIL)** - สำหรับจัดการรูปภาพ
- **PyInstaller** - สำหรับสร้างไฟล์ .exe

## 📦 การติดตั้ง

### ติดตั้ง Dependencies

```bash
pip install pillow
```

### รันโปรแกรม

```bash
python main.py
```

## 💻 การใช้งาน

1. เปิดโปรแกรม BMI Calculator
2. ปรับค่าส่วนสูง (0-220 cm) ด้วย Slider ด้านซ้าย
3. ปรับค่าน้ำหนัก (0-200 kg) ด้วย Slider ด้านขวา
4. คลิกปุ่ม "View Report" เพื่อดูผลการคำนวณ
5. โปรแกรมจะแสดงค่า BMI และผลการประเมินสุขภาพ

## 📁 โครงสร้างโปรเจค

```
.
├── main.py                 # ไฟล์หลักของโปรแกรม
├── main.spec              # ไฟล์ config สำหรับ PyInstaller
├── image/                 # โฟลเดอร์เก็บรูปภาพ
│   ├── icon.png          # ไอคอนโปรแกรม
│   ├── top.png           # ภาพส่วนหัว
│   ├── box.png           # กล่องสำหรับแสดงค่า
│   ├── scale.png         # ภาพเครื่องชั่ง
│   └── man.png           # ภาพคนที่ปรับขนาดได้
├── exe file/              # โฟลเดอร์เก็บไฟล์ .exe
│   ├── BMI calculator.exe
│   └── favicon.ico
└── build/                 # โฟลเดอร์ build จาก PyInstaller
```

## 🔨 การสร้างไฟล์ .exe

โปรเจคนี้ใช้ PyInstaller ในการสร้างไฟล์ .exe สำหรับ Windows

```bash
pyinstaller main.spec
```

หรือสร้างใหม่ด้วยคำสั่ง:

```bash
pyinstaller --onefile --windowed --icon=favicon.ico main.py
```

ไฟล์ .exe ที่สร้างเสร็จจะอยู่ในโฟลเดอร์ `dist/`

## 📝 สูตรการคำนวณ BMI

```
BMI = น้ำหนัก (kg) / (ส่วนสูง (m))²
```

## ⚠️ ข้อควรระวัง

- ตรวจสอบให้แน่ใจว่าโฟลเดอร์ `image/` และไฟล์รูปภาพทั้งหมดอยู่ในตำแหน่งที่ถูกต้อง
- ชื่อโฟลเดอร์ในโค้ดเป็น `Image/` (I ตัวใหญ่) ควรตรวจสอบให้ตรงกับชื่อโฟลเดอร์จริง

## 📄 License

โปรเจคนี้เปิดให้ใช้งานได้อย่างอิสระ

## 👨‍💻 การพัฒนาเพิ่มเติม

แนวทางในการพัฒนาต่อ:
- เพิ่มการบันทึกประวัติการคำนวณ
- เพิ่มกราฟแสดงผลการเปลี่ยนแปลงของ BMI
- รองรับหลายภาษา
- เพิ่มการคำนวณแคลอรี่ที่ควรได้รับต่อวัน
- เพิ่มคำแนะนำด้านสุขภาพตามผลการคำนวณ
